/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1', '185.146.1.93'], // Add your hostname (e.g., '127.0.0.1') here
    },
}

module.exports = nextConfig
