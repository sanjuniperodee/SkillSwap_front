/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1', 'hi-test.kz', 'www.hi-test.kz'], // Add your hostname (e.g., '127.0.0.1') here
    },
}

module.exports = nextConfig
